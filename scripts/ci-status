#!/usr/bin/env bash
# ci-status — quick view of recent GitHub Actions runs for this repo.

set -euo pipefail

LIMIT="${1:-10}"

if ! command -v gh >/dev/null 2>&1; then
  echo "gh CLI not found" >&2
  exit 1
fi

gh run list --limit "$LIMIT"
